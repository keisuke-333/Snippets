<div>
  <h1>詳細</h1>
  
  <div>
    タイトル：<%= @post.title %>
  </div>

  <div>
    言語：<%= @post.language %>
  </div>

  <div>
    コード：<%= simple_format(h @post.code) %>
  </div>
</div>
<%= link_to '一覧画面へ', posts_path %>